<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="CardViewNoBorder">
        <item name="cardCornerRadius">0dp</item> <!-- Set corner radius to 0dp for no rounded corners -->
        <item name="cardElevation">0dp</item> <!-- Remove elevation (shadow) -->
        <item name="cardBackgroundColor">@android:color/white</item> <!-- Set card background color as needed -->
        <item name="android:padding">0dp</item> <!-- Remove padding if needed -->
    </style>

    <style name="NoScrollbarStyle">
        <item name="android:fadeScrollbars">false</item>
        <item name="android:scrollbarSize">0dp</item>
        <item name="android:scrollbarThumbVertical">@null</item>
        <item name="android:scrollbarThumbHorizontal">@null</item>
    </style>

        <style name="Rounded" parent="ShapeAppearance.MaterialComponents.SmallComponent">
            <item name="cornerFamily">rounded</item>
            <item name="cornerSize">16dp</item>
        </style>

        <style name="roundedImageViewRounded">
            <item name="cornerFamily">rounded</item>
            <item name="cornerSize">50%</item>
        </style>
</resources>